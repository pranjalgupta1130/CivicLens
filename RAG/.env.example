# ==============================================================================
# CivicLens Environment Configuration
# ==============================================================================

# Gemini AI Configuration (Required for LLM reasoning & Gemini Embedding 2)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash
GEMINI_EMBEDDING_MODEL=models/text-embedding-004

# Supabase Database & pgvector (Required for production Postgres & RAG vectors)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Database direct connection (optional for local SQLAlchemy / asyncpg)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/civiclens

# Server Configuration
PORT=8000
ENVIRONMENT=development